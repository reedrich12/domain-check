"""domain-check: domain availability checker CLI (RDAP with DNS fallback).

Unit implementations land one at a time via the loop. See
.loop/ledger.json for the unit ledger (U1-U10) and checks/verify.sh for
the verification gate.
"""

__version__ = "0.1.0"
