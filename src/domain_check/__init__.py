"""domain-check: domain availability checker CLI (RDAP with DNS fallback).

Scaffold only — unit implementations land one at a time via the loop.
See .loop/ledger.json for the unit ledger (U1-U8) and checks/verify.sh
for the verification gate.
"""
